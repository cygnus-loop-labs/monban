[dictionary]
words = "data/jmdict.json"
kanji = "data/kanji.json"

[parser]
dictionary = "embedded://ipadic"

[parser.filtering]
exclude_chars = ["ー", "〜"]

[parser.filtering.include]
"名詞" = ["一般", "サ変接続"]
"動詞" = ["自立"]
"感動詞" = ["*"]

[user_decks.decks]
"kaishi" = "data/kaishi.txt"

[wk_deck]
deck = "data/wk.json"
current_level = 10
